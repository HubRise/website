import styled from "styled-components"

import { mixin } from "@utils/styles"

export const PartnerCards = styled.div`
  ${mixin.containerWrapper}
  display: flex;
  flex-direction: column;
  gap: 2rem;
`
