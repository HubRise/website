version: "2"

options:
  compose-file-path: "docker-compose.mac.yml"

syncs:
  website-sync:
    sync_strategy: "unison"
    src: "."
    sync_excludes: ['.cache', 'public', '.idea', 'node_modules' ]
